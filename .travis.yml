language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
  - '[ "${TRAVIS_PULL_REQUEST}" = "false" ] && npm test || npm run pull-test'
branches:
  except:
    - /^pr\..*/ 
   