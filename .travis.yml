language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
#- npm test
#- npm run build
- if [ "$TRAVIS_PULL_REQUEST" == true ]; then - npm test ; fi

# safelist
branches:
  only:
  - master
  
   